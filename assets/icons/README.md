# Icons

Place the Windows application icon at:

```text
assets/icons/app.ico
```

The PyInstaller spec uses this icon automatically when present. The placeholder keeps the release asset location stable until a final `.ico` is designed.

